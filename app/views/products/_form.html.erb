<div class="container">
  <div class="row">
    <div class="col-xs-6 col-xs-offset-3">
      <div class="product-form">
        <%= simple_form_for @product do |f| %>
          <%= f.input :title, placeholder: "Title", label: false, class: 'form-control' %>
          <%= f.input :price, placeholder: "Price (in €)", label: false, class: 'form-control' %>
          <%= f.input :description, placeholder: "Description", label: false, class: 'form-control' %>
          <%= f.button :submit, class: "form-control btn btn-primary" %>
        <% end %>
      </div>
    </div>
  </div>
</div>
